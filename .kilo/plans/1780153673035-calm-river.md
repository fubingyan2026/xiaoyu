# 为 foc_core/ 下文件添加详细中文注释

## 目标
为 `Q16_FOC/foc_core/` 下 6 个文件的 **C 实现文件（.c）** 中所有缺少注释的函数补充详细的中文 Doxygen 风格注释和内联说明。

## 现状分析

### 各文件注释覆盖情况

| 文件 | 函数 | 当前 .c 注释 |
|------|------|-------------|
| `foc_core.c` | `foc_core_current_loop` | ✅ 已有 |
| `foc_core.c` | `foc_core_pll_run` | ✅ 已有 |
| `foc_math.c` | `foc_sin_cos` | ⚠️ 有简短说明，需补充 LUT 实现细节 |
| `foc_math.c` | `foc_sqrt` | ✅ 已有 |
| `foc_math.c` | `foc_inv_sqrt` | ✅ 已有 |
| `foc_math.c` | `foc_atan2` | ✅ 已有 |
| `foc_math.c` | `foc_clarke_transform` | ✅ 已有 |
| `foc_math.c` | `foc_park_transform` | ✅ 已有 |
| `foc_math.c` | `foc_ipark_transform` | ✅ 已有 |
| `foc_math.c` | `foc_vector_magnitude` | ❌ **缺少** |
| `foc_math.c` | `foc_vector_magnitude_sq` | ❌ **缺少** |
| `foc_math.c` | `foc_pi_init` | ❌ **缺少** |
| `foc_math.c` | `foc_pi_calc` | ❌ **缺少** |
| `foc_math.c` | `foc_pi_reset` | ❌ **缺少** |
| `foc_math.c` | `foc_lpf_update` | ❌ **缺少** |
| `foc_math.c` | `foc_lpf_update_shift` | ❌ **缺少** |
| `foc_math.c` | `foc_ma_filter_init` | ❌ **缺少** |
| `foc_math.c` | `foc_ma_filter_update` | ❌ **缺少** |
| `foc_math.c` | `foc_normalize_angle` | ✅ 已有 |
| `foc_math.c` | `foc_normalize_angle_0_2pi` | ✅ 已有 |
| `foc_math.c` | `foc_angle_diff` | ✅ 已有 |
| `foc_svpwm.c` | `svpwm_calculate_harmonic` | ✅ 已有 |
| `foc_svpwm.c` | `foc_svpwm_init` | ✅ 已有 |
| `foc_svpwm.c` | `foc_svpwm_calculate` | ✅ 已有 |

## 实施步骤

### 1. `foc_math.c` — 补充 8 个缺失注释的函数

#### 1.1 `foc_vector_magnitude`（约第 267 行）
补充 `@brief` 描述、公式、多级溢出保护说明、使用场景。

#### 1.2 `foc_vector_magnitude_sq`（约第 277 行）
补充 `@brief` 描述、适用场景（仅需比较幅度大小时避免开方）。

#### 1.3 `foc_pi_init`（约第 284 行）
补充 `@brief`、`@param`、关键设计说明（Ki 预乘 dt 的原因）。

#### 1.4 `foc_pi_calc`（约第 298 行）
补充 `@brief`、计算公式、抗积分饱和逻辑说明。

#### 1.5 `foc_pi_reset`（约第 322 行）
补充 `@brief` 和使用场景说明。

#### 1.6 `foc_lpf_update`（约第 332 行）
补充 `@brief`、一阶低通差分方程、适用场景。

#### 1.7 `foc_lpf_update_shift`（约第 338 行）
补充 `@brief`、移位版对比普通版的使用场景、函数默认行为说明。

#### 1.8 `foc_ma_filter_init` / `foc_ma_filter_update`（约第 344 行起）
补充 `@brief`、环形缓冲区原理简述、长度必须为 2 的幂的约束说明。

### 2. `foc_math.c` — 增强 `foc_sin_cos` 注释
补充 LUT 索引计算原理（`SIN_LUT_INDEX_MULTIPLIER` 的推导）、线性插值步骤。

### 3. 其他 .c 文件
`foc_core.c` 和 `foc_svpwm.c` 各函数已有注释，可选择性增强关键内联代码块（如过调制处理、抗积分饱和）。

## 注释规范
- 遵循现有 Doxygen 风格：`@brief`、`@param`、`@return`、`@note`
- 保持中文表述
- 不修改函数逻辑，仅添加注释块
